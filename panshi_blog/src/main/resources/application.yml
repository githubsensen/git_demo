#这里写所有的配置，mysql，xml文件里的内容放到了这里


#mysql的配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 831015
    url: jdbc:mysql://localhost:3306/mybatis              #库自行修改

#    mybatis-plus的配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix:
      id-type: auto                                   #设置id自增策略
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl    #这里是设置日志方便查看


